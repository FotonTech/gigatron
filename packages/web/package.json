{
  "name": "@foton/web",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "react": "16.8.6",
    "react-art": "^16.8.6",
    "react-dom": "16.8.6",
    "react-native-web": "^0.11.2",
    "@foton/shared": "*",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "yarn tsc --build && react-app-rewired start",
    "build": "yarn tsc --build && react-app-rewired build",
    "now-build": "yarn tsc --build && react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-app-rewired eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "babel-plugin-react-native-web": "^0.11.2",
    "react-app-rewired": "^2.1.1"
  }
}
