import styled from "styled-components";

const Logo = styled.div`
    width: auto;
    height: auto;
    grid-column: 1 / 2;
    grid-row: 1 / 2;
    display: flex;
    align-items: center;
    justify-content: center;
    justify-self: center;
    align-self: flex-end;
`;

export default Logo;
